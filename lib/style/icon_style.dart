import 'package:flutter/material.dart';

Widget addIcon = const Icon(
  Icons.add_circle,
  color: Color(0xff5f66f2),
);
Widget checkIcon =
    const Icon(
        Icons.check_circle,
        size: 100,
        color: Color(0xff5f66f2)
    );
Widget removeIcon = const Icon(
    Icons.remove_circle_outline_rounded,
    size: 100,
    color: Color(0xff5f66f2)
);

Widget cancelIcon = const Icon(
  Icons.cancel,
  color: Color(0xff5f66f2),
);
